# profile decide what is the active profile
spring:
  profiles:
    active: dev
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false    

api:
  endpoint:
    base-url: /api/v1

server:
  port: 8081    